# Dockerfile simple para staging
FROM node:20-alpine

WORKDIR /app

# Copiar archivos
COPY package*.json ./
COPY . .

# Instalar y construir
RUN npm ci
RUN npm run build

# Exponer puerto
EXPOSE 3001

# Comando de inicio
CMD ["npm", "start", "--", "-p", "3001"]